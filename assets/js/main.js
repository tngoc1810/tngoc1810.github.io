
document.addEventListener("DOMContentLoaded",()=>{setupBackground();setupFilters();});
function setupBackground(){if(!document.getElementById("site-bg")){const c=document.createElement("canvas");c.id="site-bg";document.body.prepend(c)}if(!document.querySelector(".bg-orb")){const o=document.createElement("div");o.className="bg-orb";document.body.appendChild(o)}if(!document.querySelector(".bg-rings")){const r=document.createElement("div");r.className="bg-rings";document.body.appendChild(r)}if(!document.querySelector(".scanline")){const s=document.createElement("div");s.className="scanline";document.body.appendChild(s)}const canvas=document.getElementById("site-bg"),ctx=canvas.getContext("2d");let w,h,particles;function resize(){w=canvas.width=innerWidth*devicePixelRatio;h=canvas.height=innerHeight*devicePixelRatio;canvas.style.width=innerWidth+"px";canvas.style.height=innerHeight+"px";ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);particles=Array.from({length:Math.min(90,Math.floor(innerWidth/16))},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,vx:(Math.random()-.5)*.34,vy:(Math.random()-.5)*.34,r:Math.random()*2.2+1}))}function draw(){ctx.clearRect(0,0,innerWidth,innerHeight);for(let i=0;i<particles.length;i++){const p=particles[i];p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>innerWidth)p.vx*=-1;if(p.y<0||p.y>innerHeight)p.vy*=-1;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle="rgba(126,231,255,0.44)";ctx.fill();for(let j=i+1;j<particles.length;j++){const q=particles[j],dx=p.x-q.x,dy=p.y-q.y,dist=Math.sqrt(dx*dx+dy*dy);if(dist<135){ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(q.x,q.y);ctx.strokeStyle=`rgba(85,191,255,${0.12-dist/1400})`;ctx.lineWidth=1;ctx.stroke()}}}requestAnimationFrame(draw)}resize();draw();addEventListener("resize",resize)}
function setupFilters(){const chips=document.querySelectorAll("[data-filter]"),cards=document.querySelectorAll("[data-card]"),search=document.querySelector("[data-search]"),empty=document.querySelector("[data-empty]");if(!chips.length||!cards.length)return;function apply(){const active=document.querySelector("[data-filter].active"),f=active?active.dataset.filter:"all",q=search?search.value.trim().toLowerCase():"";let visible=0;cards.forEach(card=>{const cat=(card.dataset.category||"").toLowerCase(),text=((card.dataset.text||"")+" "+card.textContent).toLowerCase(),ok=(f==="all"||cat.includes(f))&&(!q||text.includes(q));card.classList.toggle("hidden",!ok);if(ok)visible++});if(empty)empty.style.display=visible?"none":"block"}chips.forEach(chip=>chip.addEventListener("click",()=>{chips.forEach(c=>c.classList.remove("active"));chip.classList.add("active");apply()}));if(search)search.addEventListener("input",apply);apply()}
